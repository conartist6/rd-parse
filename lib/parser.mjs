import {
  Debug,
  Ignore,
  IgnoreBetween,
  All,
  Any,
  Plus,
  Optional,
  Node,
  Star,
  Y,
  Parser,
} from './parser.mjs';

export { Debug, Ignore, IgnoreBetween, All, Any, Plus, Optional, Node, Star, Y };

export default Parser;
